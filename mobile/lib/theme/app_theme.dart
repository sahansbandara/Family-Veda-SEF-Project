// Owner: S3 · Triage & Agent Orchestration — Karunathilaka K.D.J.C (IT24100551)
// Ownership is binding — do not edit this file if it is not yours. docs/OWNERSHIP.tsv
// Shared design tokens from design.md — liquid glass materials.
//
// Shared design tokens from design.md — liquid glass materials.
//
// Semantic colours are unchanged: `primary` still means action, `agent` still
// marks unapproved AI output, `emergency` is still reserved for the red-flag
// path. Only the material underneath them changed.
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

final themeModeProvider = StateProvider<ThemeMode>((ref) => ThemeMode.light);

class AppColors {
  const AppColors._();

  // Light
  static const primary = Color(0xFF0D8276);
  static const primaryLum = Color(0xFF14B8A6);
  static const primarySubtle = Color(0xFFE6F7F5);
  static const primaryHover = Color(0xFF0A6B61);
  static const background = Color(0xFFEFF5F3);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceSubtle = Color(0xFFE7F1EE);
  static const border = Color(0xFFCEDFDC);
  static const borderSubtle = Color(0xFFE0EBE8);
  static const text = Color(0xFF0E1E1C);
  static const textHeading = Color(0xFF061A17);
  static const muted = Color(0xFF46605A);
  static const faint = Color(0xFF6F8984);
  static const danger = Color(0xFFB3261E);
  static const warning = Color(0xFFA66300);
  static const success = Color(0xFF167946);
  static const emergency = Color(0xFF8B0000);
  static const agent = Color(0xFF4A4A8F);
  static const onAccent = Color(0xFFFFFFFF);

  // Dark
  static const primaryDark = Color(0xFF3AA394);
  static const primaryLumDark = Color(0xFF48D8C0);
  static const primarySubtleDark = Color(0x293AA394);
  static const backgroundDark = Color(0xFF07100F);
  static const surfaceDark = Color(0xFF0F1A18);
  static const surfaceSubtleDark = Color(0xFF142421);
  static const borderDark = Color(0xFF22332F);
  static const borderSubtleDark = Color(0xFF182825);
  static const textDark = Color(0xFFE9F2F0);
  static const textHeadingDark = Color(0xFFFFFFFF);
  static const mutedDark = Color(0xFF9DB2AE);
  static const faintDark = Color(0xFF6B807C);
  static const dangerDark = Color(0xFFF2685F);
  static const warningDark = Color(0xFFE0A44A);
  static const successDark = Color(0xFF4BB574);
  static const emergencyDark = Color(0xFFFF4D4D);
  static const agentDark = Color(0xFF8C8CD9);
  static const onAccentDark = Color(0xFF06100E);

  // Ambient mesh, drawn from the brand mark.
  static const orbA = Color(0xFF86EFAC);
  static const orbB = Color(0xFF5EEAD4);
  static const orbC = Color(0xFF93C5FD);
}

/// Glass material tokens, resolved per brightness.
///
/// Exposed as a [ThemeExtension] so widgets read them off the theme rather than
/// branching on `Theme.of(context).brightness` at every call site.
@immutable
class FvGlass extends ThemeExtension<FvGlass> {
  const FvGlass({
    required this.fillHigh,
    required this.fillLow,
    required this.edge,
    required this.edgeDim,
    required this.specular,
    required this.cast,
    required this.agent,
    required this.emergency,
    required this.success,
    required this.warning,
    required this.faint,
  });

  final Color fillHigh;
  final Color fillLow;
  final Color edge;
  final Color edgeDim;
  final Color specular;
  final Color cast;
  final Color agent;
  final Color emergency;
  final Color success;
  final Color warning;
  final Color faint;

  /// Blur radii. Kept small and few on purpose — see [blurBudget].
  static const double blurThin = 9;
  static const double blurRegular = 18;
  static const double blurThick = 30;

  /// `BackdropFilter` is expensive on the mid-range Android hardware most of
  /// this app's users carry. No screen should mount more than this many live
  /// blur layers; everything else uses a tinted translucent fill, which costs
  /// nothing and is visually near-identical at card scale.
  static const int blurBudget = 3;

  static const light = FvGlass(
    fillHigh: Color(0xBDFFFFFF),
    fillLow: Color(0x66FFFFFF),
    edge: Color(0xB3FFFFFF),
    edgeDim: Color(0x1A0E322D),
    specular: Color(0xF2FFFFFF),
    cast: Color(0x3308302B),
    agent: AppColors.agent,
    emergency: AppColors.emergency,
    success: AppColors.success,
    warning: AppColors.warning,
    faint: AppColors.faint,
  );

  static const dark = FvGlass(
    fillHigh: Color(0x9E264A46),
    fillLow: Color(0x57142C29),
    edge: Color(0x337FEBD6),
    edgeDim: Color(0x66000000),
    specular: Color(0x42A0FFEE),
    cast: Color(0x85000000),
    agent: AppColors.agentDark,
    emergency: AppColors.emergencyDark,
    success: AppColors.successDark,
    warning: AppColors.warningDark,
    faint: AppColors.faintDark,
  );

  @override
  FvGlass copyWith({
    Color? fillHigh,
    Color? fillLow,
    Color? edge,
    Color? edgeDim,
    Color? specular,
    Color? cast,
    Color? agent,
    Color? emergency,
    Color? success,
    Color? warning,
    Color? faint,
  }) {
    return FvGlass(
      fillHigh: fillHigh ?? this.fillHigh,
      fillLow: fillLow ?? this.fillLow,
      edge: edge ?? this.edge,
      edgeDim: edgeDim ?? this.edgeDim,
      specular: specular ?? this.specular,
      cast: cast ?? this.cast,
      agent: agent ?? this.agent,
      emergency: emergency ?? this.emergency,
      success: success ?? this.success,
      warning: warning ?? this.warning,
      faint: faint ?? this.faint,
    );
  }

  @override
  FvGlass lerp(ThemeExtension<FvGlass>? other, double t) {
    if (other is! FvGlass) return this;
    return FvGlass(
      fillHigh: Color.lerp(fillHigh, other.fillHigh, t)!,
      fillLow: Color.lerp(fillLow, other.fillLow, t)!,
      edge: Color.lerp(edge, other.edge, t)!,
      edgeDim: Color.lerp(edgeDim, other.edgeDim, t)!,
      specular: Color.lerp(specular, other.specular, t)!,
      cast: Color.lerp(cast, other.cast, t)!,
      agent: Color.lerp(agent, other.agent, t)!,
      emergency: Color.lerp(emergency, other.emergency, t)!,
      success: Color.lerp(success, other.success, t)!,
      warning: Color.lerp(warning, other.warning, t)!,
      faint: Color.lerp(faint, other.faint, t)!,
    );
  }
}

/// Convenience accessor: `context.glass.edge`.
extension FvGlassContext on BuildContext {
  FvGlass get glass =>
      Theme.of(this).extension<FvGlass>() ?? FvGlass.light;
}

ThemeData buildAppTheme({Brightness brightness = Brightness.light}) {
  final isDark = brightness == Brightness.dark;

  final primary = isDark ? AppColors.primaryDark : AppColors.primary;
  final surface = isDark ? AppColors.surfaceDark : AppColors.surface;
  final background = isDark ? AppColors.backgroundDark : AppColors.background;
  final text = isDark ? AppColors.textDark : AppColors.text;
  final textHeading = isDark ? AppColors.textHeadingDark : AppColors.textHeading;
  final muted = isDark ? AppColors.mutedDark : AppColors.muted;
  final border = isDark ? AppColors.borderDark : AppColors.border;
  final danger = isDark ? AppColors.dangerDark : AppColors.danger;
  final onPrimary = isDark ? const Color(0xFF06100E) : Colors.white;

  final scheme = ColorScheme.fromSeed(
    seedColor: primary,
    brightness: brightness,
    primary: primary,
    onPrimary: onPrimary,
    surface: surface,
    error: danger,
  );

  return ThemeData(
    useMaterial3: true,
    colorScheme: scheme,
    brightness: brightness,
    // Transparent so the ambient mesh shows through; AmbientBackground paints
    // the ground colour itself.
    scaffoldBackgroundColor: background,
    extensions: <ThemeExtension<dynamic>>[
      isDark ? FvGlass.dark : FvGlass.light,
    ],
    textTheme: TextTheme(
      displaySmall: TextStyle(
        fontWeight: FontWeight.w800,
        letterSpacing: -0.5,
        height: 1.08,
        color: textHeading,
      ),
      headlineSmall: TextStyle(
        fontWeight: FontWeight.w700,
        letterSpacing: -0.3,
        color: textHeading,
      ),
      titleLarge: TextStyle(fontWeight: FontWeight.w700, color: textHeading),
      titleMedium: TextStyle(fontWeight: FontWeight.w600, color: text),
      bodyLarge: TextStyle(fontSize: 16, height: 1.55, color: text),
      bodyMedium: TextStyle(fontSize: 15, height: 1.55, color: text),
      bodySmall: TextStyle(fontSize: 13, height: 1.45, color: muted),
      labelLarge: TextStyle(fontWeight: FontWeight.w600, color: text),
    ),
    appBarTheme: AppBarTheme(
      backgroundColor: Colors.transparent,
      surfaceTintColor: Colors.transparent,
      foregroundColor: text,
      elevation: 0,
      scrolledUnderElevation: 0,
      centerTitle: false,
      titleTextStyle: TextStyle(
        fontSize: 18,
        fontWeight: FontWeight.w600,
        letterSpacing: -0.2,
        color: text,
      ),
    ),
    cardTheme: CardThemeData(
      color: surface.withValues(alpha: isDark ? 0.55 : 0.72),
      surfaceTintColor: Colors.transparent,
      elevation: 0,
      margin: const EdgeInsets.only(bottom: 10),
      shape: RoundedRectangleBorder(
        side: BorderSide(color: border),
        borderRadius: const BorderRadius.all(Radius.circular(20)),
      ),
    ),
    listTileTheme: ListTileThemeData(
      iconColor: primary,
      textColor: text,
      shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.all(Radius.circular(20)),
      ),
    ),
    dividerTheme: DividerThemeData(color: border, thickness: 1, space: 1),
    chipTheme: ChipThemeData(
      backgroundColor: surface.withValues(alpha: isDark ? 0.4 : 0.6),
      side: BorderSide(color: border),
      labelStyle: TextStyle(fontWeight: FontWeight.w500, color: text),
      shape: const StadiumBorder(),
    ),
    inputDecorationTheme: InputDecorationTheme(
      filled: true,
      fillColor: surface.withValues(alpha: isDark ? 0.45 : 0.66),
      hintStyle: TextStyle(color: muted),
      labelStyle: TextStyle(color: muted),
      border: OutlineInputBorder(
        borderRadius: const BorderRadius.all(Radius.circular(14)),
        borderSide: BorderSide(color: border),
      ),
      enabledBorder: OutlineInputBorder(
        borderRadius: const BorderRadius.all(Radius.circular(14)),
        borderSide: BorderSide(color: border),
      ),
      focusedBorder: OutlineInputBorder(
        borderRadius: const BorderRadius.all(Radius.circular(14)),
        borderSide: BorderSide(color: primary, width: 2),
      ),
    ),
    elevatedButtonTheme: ElevatedButtonThemeData(
      style: ElevatedButton.styleFrom(
        minimumSize: const Size(44, 50),
        backgroundColor: primary,
        foregroundColor: onPrimary,
        elevation: 0,
        textStyle: const TextStyle(fontSize: 15, fontWeight: FontWeight.w600),
        shape: const RoundedRectangleBorder(
          borderRadius: BorderRadius.all(Radius.circular(14)),
        ),
      ),
    ),
    outlinedButtonTheme: OutlinedButtonThemeData(
      style: OutlinedButton.styleFrom(
        minimumSize: const Size(44, 50),
        foregroundColor: text,
        side: BorderSide(color: border),
        shape: const RoundedRectangleBorder(
          borderRadius: BorderRadius.all(Radius.circular(14)),
        ),
      ),
    ),
    textButtonTheme: TextButtonThemeData(
      style: TextButton.styleFrom(foregroundColor: primary),
    ),
    snackBarTheme: SnackBarThemeData(
      behavior: SnackBarBehavior.floating,
      shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.all(Radius.circular(14)),
      ),
      backgroundColor: isDark ? AppColors.surfaceDark : AppColors.text,
    ),
  );
}
