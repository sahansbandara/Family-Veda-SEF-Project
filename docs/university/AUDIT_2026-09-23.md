# SE3090 compliance audit — 2026-09-23

Basis: current `develop` checkout after syncing `origin/develop`, inspected repository files, and the university PDFs in [requirements reference](REQUIREMENTS_REFERENCE.md). **No new CI run, authenticated E2E run, physical-device test, performance test, or security scan was executed for this audit.** Code presence is not a pass result.

| Priority | PDF requirement | Current evidence | Status and next proof |
|---|---|---|---|
| P0 | Full integrated agent workflow, human approval and safe failure (A1 pp. 6, 8, 15; A2 p. 1) | Agent/orchestration tests and hosted LLM fallback code exist. No executed Flutter → API/DB/agents → React approval → Flutter trace was found in this checkout. | **Unverified.** Run one synthetic golden case and one safe-failure case; retain plan, tool, validation, approval and patient-visible result evidence. |
| P0 | Live API health and Swagger URLs (A1 pp. 9–10) | `render.yaml` and `docs/DEPLOYMENT.md` exist. Live deployment matching this branch was not verified. | **Unverified.** Check deployed revision, `/health`, `/swagger` and authenticated endpoint in private browser. |
| P0 | A2 performance and security tests with tools (A2 pp. 1–2) | No k6/Playwright E2E/security scan scripts or generated outputs found in this checkout. | **Missing evidence.** Execute suitable tools and record actual results and interpretation. |
| P0 | Completed A2 test documents and defect/retest evidence (A2 pp. 2–3) | No completed test-case document, defect report, execution summary or testing-report PDF found in this checkout. | **Missing evidence.** Produce from real executed tests; do not invent results or defects. |
| P1 | Backend, database, React and Flutter tests (A1 p. 8; A2 pp. 1–2) | Unit/integration backend tests, React tests, Flutter tests and `.github/workflows/ci.yml` exist. | **Code present.** Run CI and collect output, coverage, database, UI and agent evaluation evidence. |
| P1 | Meaningful third-party integration (A1 p. 8) | Hosted LLM fallback and deployment configuration exist; no successful external call or failure-path result was verified here. | **Unverified.** Test synthetic success, timeout/rate-limit and failure behavior. |
| P1 | Android APK and device evidence (A1 pp. 9–10) | Flutter source exists; no release APK artifact or physical Android installation evidence found in this checkout. | **Missing evidence.** Build with hosted API, install and document result. |
| P1 | Individual ownership, AI logs and reflection (A1 pp. 3–4, 10, 16–17) | `docs/individual-reports/` exists. No ownership, genuine AI-log or student-authored reflection was verified by this audit. | **Unverified.** Each owner supplies their own commits/tests/log/reflection; no agent writes a student's reflection. |
| P1 | Search, filter, sorting, pagination and analytics (A1 pp. 4–5) | No completed endpoint/UI coverage audit was run on this checkout. | **Unverified.** Check each owner component against API and UI tests. |

## Evidence package to finish

1. Exact deployed revision and accessible API health, Swagger, web, database migration and authenticated-role evidence.
2. Synthetic cross-platform case with persisted plan, distinct agent traces, allow-listed tool calls, deterministic validation, doctor approval, safe failure and no unapproved patient output.
3. Real backend/DB/React/Flutter/E2E, performance, security and agent evaluation results; A2 test plan, cases, defects, retests and execution summary.
4. Android APK/device result, third-party success/failure evidence, individual Git/AI evidence, consolidated report and public video link.

Do not mark a row complete from a source file alone. Re-check this dated audit when code or deployment changes.
