# S2 — push plan for `Family-Veda-SEF-Project`

**Fernando K.R.N** · IT24101875 · `@it24101875`  
**Component:** Health Records & Extraction  
**Branch:** `feature/s2-lab-ocr-extraction`  
**Target repository:** https://github.com/sahansbandara/Family-Veda-SEF-Project

> Generated from [`docs/OWNERSHIP.tsv`](../OWNERSHIP.tsv). Every path below is a real file in the source repository — nothing here is invented. Push **backend first**, in the phase order given.

## Rules — read once, they decide your marks

| | |
|---|---|
| **Your identity** | `git config user.name "Fernando K.R.N"` and `user.email` = the address on `@it24101875`. Verify with `git log --author` before every push |
| **Push your own work only** | Never `--author` someone else, never commit on a teammate's behalf. 70 of 100 marks are individual and are read from `git log --author` |
| **Never backdate** | No `--date` rewriting. A fabricated timeline is worse than a short one |
| **Ownership** | Touch only files listed in this document. Everything else belongs to another member |
| **Shared files** | Add lines inside your own labelled block. Never reorder or reformat |
| **Migrations** | Announce the lock, one migration at a time, push immediately, release the lock |
| **Safety** | Synthetic data only. No real patient data, no drug names, no dosing, no diagnosis, no meal plans — in code, comments or seed data |

## Phase 0 — wait for the bootstrap

S1 pushes the repository skeleton — solution, the six `.csproj` files, the ⚠ SHARED spine (`Program.cs`, `DependencyInjection.cs`, `AppDbContext.cs`), `ci.yml`, and the ownership manifest.

**Do not push before S1 announces that the bootstrap is in.** Then:

```bash
git clone https://github.com/sahansbandara/Family-Veda-SEF-Project.git
cd Family-Veda-SEF-Project
git config user.name "Fernando K.R.N"
git config user.email "<the email on your GitHub account>"
git checkout -b feature/s2-lab-ocr-extraction
```

## Phase 1 — Domain entities and policies

- `backend/src/Domain/Records/RecordEntities.cs`

```bash
git add \
  backend/src/Domain/Records/RecordEntities.cs
git commit -m "feat(s2): add the health records and extraction domain entities"
git push -u origin feature/s2-lab-ocr-extraction
```

## Phase 2 — Application contracts and DTOs

- `backend/src/Application/Records/RecordContracts.cs`

```bash
git add \
  backend/src/Application/Records/RecordContracts.cs
git commit -m "feat(s2): define the health records and extraction DTOs and contracts"
git push -u origin feature/s2-lab-ocr-extraction
```

## Phase 3 — Persistence — EF Core configuration

- `backend/src/Infrastructure/Persistence/Configurations/RecordConfigurations.cs`

```bash
git add \
  backend/src/Infrastructure/Persistence/Configurations/RecordConfigurations.cs
git commit -m "feat(s2): configure the health records and extraction tables, indexes and constraints"
git push -u origin feature/s2-lab-ocr-extraction
```

> ⚠ **Migration lock.** Before `dotnet ef migrations add`, announce it in the group chat, `git pull`, add exactly one migration, run `dotnet ef database update`, push immediately, then release the lock. Never two in flight. Name it `<date>_S2_<Change>`.

## Phase 4 — Infrastructure — service layer

- `backend/src/Infrastructure/Records/LabExtractionService.cs`
- `backend/src/Infrastructure/Records/RecordService.cs`
- `backend/src/Infrastructure/Records/TesseractOcrService.cs`

```bash
git add \
  backend/src/Infrastructure/Records/LabExtractionService.cs \
  backend/src/Infrastructure/Records/RecordService.cs \
  backend/src/Infrastructure/Records/TesseractOcrService.cs
git commit -m "feat(s2): implement the health records and extraction service layer"
git push -u origin feature/s2-lab-ocr-extraction
```

## Phase 5 — wire your slice into the ⚠ SHARED spine

Your entities and services exist but nothing references them yet. Add **only your own labelled block** to each file below. Never reorder or reformat lines that are already there — that turns a clean merge into a conflict for everyone.

| File | What you add |
|---|---|
| `backend/src/Infrastructure/Persistence/AppDbContext.cs` | the `DbSet<>` properties for `health_records` `lab_reports` `lab_values` `vitals` `hereditary_flags` |
| `backend/src/Infrastructure/DependencyInjection.cs` | the `services.AddScoped<>` registrations for your services |
| `backend/src/Api/Program.cs` | any middleware or options your slice needs |

```bash
git add \
  backend/src/Infrastructure/Persistence/AppDbContext.cs \
  backend/src/Infrastructure/DependencyInjection.cs
git commit -m "feat(s2): register the health records and extraction entities and services"
git push origin feature/s2-lab-ocr-extraction
```

**Gate:** `dotnet build` clean. Until all four members have pushed this phase the solution will not compile end to end — expect red CI in between, and say so on the PR rather than hiding it.

## Phase 6 — Agent layer

- `backend/src/Infrastructure/Agents/ExtractionAgent.cs`

```bash
git add \
  backend/src/Infrastructure/Agents/ExtractionAgent.cs
git commit -m "feat(s2): add the extraction agent"
git push -u origin feature/s2-lab-ocr-extraction
```

## Phase 7 — API — controllers and workers

- `backend/src/Api/Controllers/RecordsController.cs`

```bash
git add \
  backend/src/Api/Controllers/RecordsController.cs
git commit -m "feat(s2): expose the health records and extraction endpoints"
git push -u origin feature/s2-lab-ocr-extraction
```

**Gate:** Every endpoint returns 2xx in Swagger

## Phase 8 — Backend tests

- `backend/tests/UnitTests/LabExtractionParserTests.cs`
- `backend/tests/UnitTests/LabExtractionSafetyTests.cs`
- `backend/tests/UnitTests/LabReportDurableStorageTests.cs`
- `backend/tests/UnitTests/RecordServiceLabReviewTests.cs`

```bash
git add \
  backend/tests/UnitTests/LabExtractionParserTests.cs \
  backend/tests/UnitTests/LabExtractionSafetyTests.cs \
  backend/tests/UnitTests/LabReportDurableStorageTests.cs \
  backend/tests/UnitTests/RecordServiceLabReviewTests.cs
git commit -m "test(s2): cover the health records and extraction service and policy layer"
git push -u origin feature/s2-lab-ocr-extraction
```

**Gate:** `dotnet build` clean and `dotnet test` green — **backend gate**

## Phase 9 — React web application

- `web/src/pages/records/RecordsPage.test.tsx`
- `web/src/pages/records/RecordsPage.tsx`

```bash
git add \
  web/src/pages/records/RecordsPage.test.tsx \
  web/src/pages/records/RecordsPage.tsx
git commit -m "feat(s2): add the health records and extraction web screens"
git push -u origin feature/s2-lab-ocr-extraction
```

## Phase 10 — Flutter mobile application

- `mobile/lib/models/health_record.dart`
- `mobile/lib/providers/records_provider.dart`
- `mobile/lib/screens/records/lab_upload_screen.dart`
- `mobile/lib/screens/records/record_entry_screen.dart`
- `mobile/lib/screens/records/records_screen.dart`
- `mobile/lib/screens/records/vital_entry_screen.dart`
- `mobile/lib/services/api/mobile_api.dart`

```bash
git add \
  mobile/lib/models/health_record.dart \
  mobile/lib/providers/records_provider.dart \
  mobile/lib/screens/records/lab_upload_screen.dart \
  mobile/lib/screens/records/record_entry_screen.dart \
  mobile/lib/screens/records/records_screen.dart \
  mobile/lib/screens/records/vital_entry_screen.dart \
  mobile/lib/services/api/mobile_api.dart
git commit -m "feat(s2): add the health records and extraction mobile screens"
git push -u origin feature/s2-lab-ocr-extraction
```

## Phase 11 — Flutter tests

- `mobile/test/screens/records_screen_test.dart`

```bash
git add \
  mobile/test/screens/records_screen_test.dart
git commit -m "test(s2): cover the health records and extraction providers and screens"
git push -u origin feature/s2-lab-ocr-extraction
```

**Gate:** `flutter analyze` clean and `flutter test` green

## Your agentic contribution (12 marks)

**Extraction Agent** — stage 1 of the two-stage model (ADR-003). Scoped to one member. OCR → parse → write a structured `hereditary_flags` row. Raw record content never leaves this stage.

## Phase 12 — documentation and evidence

| Stage | Commit |
|---|---|
| `docs/individual-reports/S2.md` §1–§4 | `docs(s2): complete report sections 1 to 4 with evidence` |
| `docs/individual-reports/S2.md` §5–§7 + `git log --author` output | `docs(s2): complete report sections 5 to 7 with evidence` |
| `docs/ai-disclosure/S2.md` | `docs(s2): finalise the AI use disclosure log` |
| `docs/individual-reports/S2.md` §8 — **written by you, never AI** | `docs(s2): add personal reflection` |

> §8 must be your own words. An AI-generated reflection receives no credit.

## Summary

| | |
|---|---|
| Files you own | 22 |
| ⚠ SHARED files you coordinate | 0 |
| Database tables | `health_records` `lab_reports` `lab_values` `vitals` `hereditary_flags` |
| Endpoints | `/records/*` `/lab-reports/*` `/vitals/*` `/hereditary-flags` |
| Commits in this plan | 12 phases, ~16 commits minimum |

## Verify before you call it done

```bash
# your commits, and only yours
git log --author="Fernando" --oneline | wc -l

# nothing of yours is untagged or unowned
grep -E '^S2\s' docs/OWNERSHIP.tsv | cut -f2 | \
  xargs -I{} git log -1 --format="%an  {}" -- {}

# full stack still builds
cd backend && dotnet build && dotnet test
cd ../web && npm ci && npm run build && npx vitest run
cd ../mobile && flutter pub get && flutter analyze && flutter test
```

