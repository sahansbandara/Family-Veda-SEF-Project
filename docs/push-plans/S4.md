# S4 — push plan for `Family-Veda-SEF-Project`

**W.M.S.S.B. Wasala** · IT24100559 · `@sahansbandara`  
**Component:** Familial Risk & Clinical Approval  
**Branch:** `feature/s4-approval-gate`  
**Target repository:** https://github.com/sahansbandara/Family-Veda-SEF-Project

> Generated from [`docs/OWNERSHIP.tsv`](../OWNERSHIP.tsv). Every path below is a real file in the source repository — nothing here is invented. Push **backend first**, in the phase order given.

## Rules — read once, they decide your marks

| | |
|---|---|
| **Your identity** | `git config user.name "W.M.S.S.B. Wasala"` and `user.email` = the address on `@sahansbandara`. Verify with `git log --author` before every push |
| **Push your own work only** | Never `--author` someone else, never commit on a teammate's behalf. 70 of 100 marks are individual and are read from `git log --author` |
| **Never backdate** | No `--date` rewriting. A fabricated timeline is worse than a short one |
| **Ownership** | Touch only files listed in this document. Everything else belongs to another member |
| **Shared files** | Add lines inside your own labelled block. Never reorder or reformat |
| **Migrations** | Announce the lock, one migration at a time, push immediately, release the lock |
| **Safety** | Synthetic data only. No real patient data, no drug names, no dosing, no diagnosis, no meal plans — in code, comments or seed data |

## Phase 0 — wait for the bootstrap

S1 pushes the repository skeleton — solution, the six `.csproj` files, the ⚠ SHARED spine (`Program.cs`, `DependencyInjection.cs`, `AppDbContext.cs`), `ci.yml`, and the ownership manifest.

**Do not push before S1 announces that the bootstrap is in.** Then:

```bash
git clone https://github.com/sahansbandara/Family-Veda-SEF-Project.git
cd Family-Veda-SEF-Project
git config user.name "W.M.S.S.B. Wasala"
git config user.email "<the email on your GitHub account>"
git checkout -b feature/s4-approval-gate
```

## Phase 1 — Domain entities and policies

- `backend/src/Domain/Access/CaseGrantPolicy.cs`
- `backend/src/Domain/Clinical/ClinicalEntities.cs`
- `backend/src/Domain/FamilialRisk/FamilialRiskPolicy.cs`
- `backend/src/Domain/Safety/ClinicalRuleTables.cs`
- `backend/src/Domain/Safety/SafetyValidationService.cs`

```bash
git add \
  backend/src/Domain/Access/CaseGrantPolicy.cs \
  backend/src/Domain/Clinical/ClinicalEntities.cs \
  backend/src/Domain/FamilialRisk/FamilialRiskPolicy.cs \
  backend/src/Domain/Safety/ClinicalRuleTables.cs \
  backend/src/Domain/Safety/SafetyValidationService.cs
git commit -m "feat(s4): add the familial risk and clinical approval domain entities"
git push -u origin feature/s4-approval-gate
```

## Phase 2 — Application contracts and DTOs

- `backend/src/Application/Clinical/ClinicalContracts.cs`

```bash
git add \
  backend/src/Application/Clinical/ClinicalContracts.cs
git commit -m "feat(s4): define the familial risk and clinical approval DTOs and contracts"
git push -u origin feature/s4-approval-gate
```

## Phase 3 — Persistence — EF Core configuration

- `backend/src/Infrastructure/Persistence/Configurations/ClinicalConfigurations.cs`

```bash
git add \
  backend/src/Infrastructure/Persistence/Configurations/ClinicalConfigurations.cs
git commit -m "feat(s4): configure the familial risk and clinical approval tables, indexes and constraints"
git push -u origin feature/s4-approval-gate
```

> ⚠ **Migration lock.** Before `dotnet ef migrations add`, announce it in the group chat, `git pull`, add exactly one migration, run `dotnet ef database update`, push immediately, then release the lock. Never two in flight. Name it `<date>_S4_<Change>`.

## Phase 4 — Infrastructure — service layer

- `backend/src/Infrastructure/Clinical/ClinicalService.cs`

```bash
git add \
  backend/src/Infrastructure/Clinical/ClinicalService.cs
git commit -m "feat(s4): implement the familial risk and clinical approval service layer"
git push -u origin feature/s4-approval-gate
```

## Phase 5 — wire your slice into the ⚠ SHARED spine

Your entities and services exist but nothing references them yet. Add **only your own labelled block** to each file below. Never reorder or reformat lines that are already there — that turns a clean merge into a conflict for everyone.

| File | What you add |
|---|---|
| `backend/src/Infrastructure/Persistence/AppDbContext.cs` | the `DbSet<>` properties for `doctors` `doctor_verification_log` `family_doctor_assignments` `case_access_grants` `approvals` `audit_log` |
| `backend/src/Infrastructure/DependencyInjection.cs` | the `services.AddScoped<>` registrations for your services |
| `backend/src/Api/Program.cs` | any middleware or options your slice needs |

```bash
git add \
  backend/src/Infrastructure/Persistence/AppDbContext.cs \
  backend/src/Infrastructure/DependencyInjection.cs
git commit -m "feat(s4): register the familial risk and clinical approval entities and services"
git push origin feature/s4-approval-gate
```

**Gate:** `dotnet build` clean. Until all four members have pushed this phase the solution will not compile end to end — expect red CI in between, and say so on the PR rather than hiding it.

## Phase 6 — Agent layer

- `backend/src/Infrastructure/Agents/FamilialRiskAgent.cs`

```bash
git add \
  backend/src/Infrastructure/Agents/FamilialRiskAgent.cs
git commit -m "feat(s4): add the familial risk and safety validation agents"
git push -u origin feature/s4-approval-gate
```

## Phase 7 — API — controllers and workers

- `backend/src/Api/Controllers/ClinicalController.cs`

```bash
git add \
  backend/src/Api/Controllers/ClinicalController.cs
git commit -m "feat(s4): expose the familial risk and clinical approval endpoints"
git push -u origin feature/s4-approval-gate
```

**Gate:** Every endpoint returns 2xx in Swagger

## Phase 8 — Backend tests

- `backend/tests/UnitTests/CaseGrantPolicyTests.cs`
- `backend/tests/UnitTests/ClinicalCasePoolPrivacyTests.cs`
- `backend/tests/UnitTests/ClinicalEmergencyReferralTests.cs`
- `backend/tests/UnitTests/ClinicalRuleTableTests.cs`
- `backend/tests/UnitTests/FamilialRiskPolicyTests.cs`
- `backend/tests/UnitTests/SafetyValidationServiceTests.cs`

```bash
git add \
  backend/tests/UnitTests/CaseGrantPolicyTests.cs \
  backend/tests/UnitTests/ClinicalCasePoolPrivacyTests.cs \
  backend/tests/UnitTests/ClinicalEmergencyReferralTests.cs \
  backend/tests/UnitTests/ClinicalRuleTableTests.cs \
  backend/tests/UnitTests/FamilialRiskPolicyTests.cs \
  backend/tests/UnitTests/SafetyValidationServiceTests.cs
git commit -m "test(s4): cover the familial risk and clinical approval service and policy layer"
git push -u origin feature/s4-approval-gate
```

**Gate:** `dotnet build` clean and `dotnet test` green — **backend gate**

## Phase 9 — React web application

- `web/src/pages/admin/DoctorVerificationPage.tsx`
- `web/src/pages/audit/AuditPage.tsx`
- `web/src/pages/auth/DoctorRegisterPage.tsx`
- `web/src/pages/doctor/ApprovalsPage.test.tsx`
- `web/src/pages/doctor/ApprovalsPage.tsx`
- `web/src/pages/doctor/CasesPage.tsx`
- `web/src/pages/doctor/DoctorStatusPage.tsx`
- `web/src/pages/system/SystemPages.tsx`

```bash
git add \
  web/src/pages/admin/DoctorVerificationPage.tsx \
  web/src/pages/audit/AuditPage.tsx \
  web/src/pages/auth/DoctorRegisterPage.tsx \
  web/src/pages/doctor/ApprovalsPage.test.tsx \
  web/src/pages/doctor/ApprovalsPage.tsx \
  web/src/pages/doctor/CasesPage.tsx \
  web/src/pages/doctor/DoctorStatusPage.tsx \
  web/src/pages/system/SystemPages.tsx
git commit -m "feat(s4): add the familial risk and clinical approval web screens"
git push -u origin feature/s4-approval-gate
```

## Phase 10 — Flutter mobile application

- `mobile/lib/models/approved_guidance.dart`
- `mobile/lib/providers/guidance_provider.dart`
- `mobile/lib/screens/emergency/emergency_screen.dart`
- `mobile/lib/screens/risk/approved_guidance_screen.dart`
- `mobile/lib/widgets/shared/clinical_disclaimer.dart`

```bash
git add \
  mobile/lib/models/approved_guidance.dart \
  mobile/lib/providers/guidance_provider.dart \
  mobile/lib/screens/emergency/emergency_screen.dart \
  mobile/lib/screens/risk/approved_guidance_screen.dart \
  mobile/lib/widgets/shared/clinical_disclaimer.dart
git commit -m "feat(s4): add the familial risk and clinical approval mobile screens"
git push -u origin feature/s4-approval-gate
```

## Phase 11 — Flutter tests

- `mobile/test/models/approved_guidance_test.dart`
- `mobile/test/providers/guidance_provider_test.dart`
- `mobile/test/screens/approved_guidance_screen_test.dart`
- `mobile/test/screens/emergency_screen_test.dart`

```bash
git add \
  mobile/test/models/approved_guidance_test.dart \
  mobile/test/providers/guidance_provider_test.dart \
  mobile/test/screens/approved_guidance_screen_test.dart \
  mobile/test/screens/emergency_screen_test.dart
git commit -m "test(s4): cover the familial risk and clinical approval providers and screens"
git push -u origin feature/s4-approval-gate
```

**Gate:** `flutter analyze` clean and `flutter test` green

## Your agentic contribution (12 marks)

**Familial Risk + Safety/Validation agents.** Stage 2 of the two-stage model: consumes S2's flags, never the underlying records. Clinical safety checks are deterministic, never LLM judgement (rule 4).

## Phase 12 — documentation and evidence

| Stage | Commit |
|---|---|
| `docs/individual-reports/S4.md` §1–§4 | `docs(s4): complete report sections 1 to 4 with evidence` |
| `docs/individual-reports/S4.md` §5–§7 + `git log --author` output | `docs(s4): complete report sections 5 to 7 with evidence` |
| `docs/ai-disclosure/S4.md` | `docs(s4): finalise the AI use disclosure log` |
| `docs/individual-reports/S4.md` §8 — **written by you, never AI** | `docs(s4): add personal reflection` |

> §8 must be your own words. An AI-generated reflection receives no credit.

## Summary

| | |
|---|---|
| Files you own | 33 |
| ⚠ SHARED files you coordinate | 0 |
| Database tables | `doctors` `doctor_verification_log` `family_doctor_assignments` `case_access_grants` `approvals` `audit_log` |
| Endpoints | `/doctors/*` `/approvals/*` `/case-grants/*` `/audit/*` |
| Commits in this plan | 12 phases, ~16 commits minimum |

## Verify before you call it done

```bash
# your commits, and only yours
git log --author="W.M.S.S.B." --oneline | wc -l

# nothing of yours is untagged or unowned
grep -E '^S4\s' docs/OWNERSHIP.tsv | cut -f2 | \
  xargs -I{} git log -1 --format="%an  {}" -- {}

# full stack still builds
cd backend && dotnet build && dotnet test
cd ../web && npm ci && npm run build && npx vitest run
cd ../mobile && flutter pub get && flutter analyze && flutter test
```

