# S3 — push plan for `Family-Veda-SEF-Project`

**Karunathilaka K.D.J.C** · IT24100551 · `@Jani6969`  
**Component:** Triage & Agent Orchestration  
**Branch:** `feature/s3-agent-orchestration`  
**Target repository:** https://github.com/sahansbandara/Family-Veda-SEF-Project

> Generated from [`docs/OWNERSHIP.tsv`](../OWNERSHIP.tsv). Every path below is a real file in the source repository — nothing here is invented. Push **backend first**, in the phase order given.

## Rules — read once, they decide your marks

| | |
|---|---|
| **Your identity** | `git config user.name "Karunathilaka K.D.J.C"` and `user.email` = the address on `@Jani6969`. Verify with `git log --author` before every push |
| **Push your own work only** | Never `--author` someone else, never commit on a teammate's behalf. 70 of 100 marks are individual and are read from `git log --author` |
| **Never backdate** | No `--date` rewriting. A fabricated timeline is worse than a short one |
| **Ownership** | Touch only files listed in this document. Everything else belongs to another member |
| **Shared files** | Add lines inside your own labelled block. Never reorder or reformat |
| **Migrations** | Announce the lock, one migration at a time, push immediately, release the lock |
| **Safety** | Synthetic data only. No real patient data, no drug names, no dosing, no diagnosis, no meal plans — in code, comments or seed data |

## Phase 0 — wait for the bootstrap

S1 pushes the repository skeleton — solution, the six `.csproj` files, the ⚠ SHARED spine (`Program.cs`, `DependencyInjection.cs`, `AppDbContext.cs`), `ci.yml`, and the ownership manifest.

**Do not push before S1 announces that the bootstrap is in.** Then:

```bash
git clone https://github.com/sahansbandara/Family-Veda-SEF-Project.git
cd Family-Veda-SEF-Project
git config user.name "Karunathilaka K.D.J.C"
git config user.email "<the email on your GitHub account>"
git checkout -b feature/s3-agent-orchestration
```

## Phase 1 — Domain entities and policies

- `backend/src/Domain/Triage/TriageEntities.cs`

```bash
git add \
  backend/src/Domain/Triage/TriageEntities.cs
git commit -m "feat(s3): add the triage and agent orchestration domain entities"
git push -u origin feature/s3-agent-orchestration
```

## Phase 2 — Application contracts and DTOs

- `backend/src/Application/Triage/TriageContracts.cs`

```bash
git add \
  backend/src/Application/Triage/TriageContracts.cs
git commit -m "feat(s3): define the triage and agent orchestration DTOs and contracts"
git push -u origin feature/s3-agent-orchestration
```

## Phase 3 — Persistence — EF Core configuration

- `backend/src/Infrastructure/Persistence/Configurations/TriageConfigurations.cs`

```bash
git add \
  backend/src/Infrastructure/Persistence/Configurations/TriageConfigurations.cs
git commit -m "feat(s3): configure the triage and agent orchestration tables, indexes and constraints"
git push -u origin feature/s3-agent-orchestration
```

> ⚠ **Migration lock.** Before `dotnet ef migrations add`, announce it in the group chat, `git pull`, add exactly one migration, run `dotnet ef database update`, push immediately, then release the lock. Never two in flight. Name it `<date>_S3_<Change>`.

## Phase 4 — Infrastructure — service layer

- `backend/src/Infrastructure/Triage/CaseSlaProcessor.cs`
- `backend/src/Infrastructure/Triage/FcmPushNotificationClient.cs`
- `backend/src/Infrastructure/Triage/NotificationService.cs`
- `backend/src/Infrastructure/Triage/TriageOrchestrator.cs`
- `backend/src/Infrastructure/Triage/TriageService.cs`
- `backend/src/Infrastructure/Triage/TriageWorkQueue.cs`

```bash
git add \
  backend/src/Infrastructure/Triage/CaseSlaProcessor.cs \
  backend/src/Infrastructure/Triage/FcmPushNotificationClient.cs \
  backend/src/Infrastructure/Triage/NotificationService.cs \
  backend/src/Infrastructure/Triage/TriageOrchestrator.cs \
  backend/src/Infrastructure/Triage/TriageService.cs \
  backend/src/Infrastructure/Triage/TriageWorkQueue.cs
git commit -m "feat(s3): implement the triage and agent orchestration service layer"
git push -u origin feature/s3-agent-orchestration
```

## Phase 5 — wire your slice into the ⚠ SHARED spine

Your entities and services exist but nothing references them yet. Add **only your own labelled block** to each file below. Never reorder or reformat lines that are already there — that turns a clean merge into a conflict for everyone.

| File | What you add |
|---|---|
| `backend/src/Infrastructure/Persistence/AppDbContext.cs` | the `DbSet<>` properties for `episodes` `triage_cases` `agent_traces` `notification_subscriptions` |
| `backend/src/Infrastructure/DependencyInjection.cs` | the `services.AddScoped<>` registrations for your services |
| `backend/src/Api/Program.cs` | any middleware or options your slice needs |

```bash
git add \
  backend/src/Infrastructure/Persistence/AppDbContext.cs \
  backend/src/Infrastructure/DependencyInjection.cs
git commit -m "feat(s3): register the triage and agent orchestration entities and services"
git push origin feature/s3-agent-orchestration
```

**Gate:** `dotnet build` clean. Until all four members have pushed this phase the solution will not compile end to end — expect red CI in between, and say so on the PR rather than hiding it.

## Phase 6 — Agent layer

- `backend/src/Infrastructure/Agents/AnalysisAgent.cs`
- `backend/src/Infrastructure/Agents/ChatCompletionsLlmClient.cs`
- `backend/src/Infrastructure/Agents/ContextAgent.cs`
- `backend/src/Infrastructure/Agents/GeminiClient.cs`
- `backend/src/Infrastructure/Agents/OllamaClient.cs`

```bash
git add \
  backend/src/Infrastructure/Agents/AnalysisAgent.cs \
  backend/src/Infrastructure/Agents/ChatCompletionsLlmClient.cs \
  backend/src/Infrastructure/Agents/ContextAgent.cs \
  backend/src/Infrastructure/Agents/GeminiClient.cs \
  backend/src/Infrastructure/Agents/OllamaClient.cs
git commit -m "feat(s3): add the coordinator, context and analysis agents"
git push -u origin feature/s3-agent-orchestration
```

## Phase 7 — API — controllers and workers

- `backend/src/Api/Background/CaseSlaWorker.cs`
- `backend/src/Api/Background/TriageWorker.cs`
- `backend/src/Api/Controllers/TriageController.cs`

```bash
git add \
  backend/src/Api/Background/CaseSlaWorker.cs \
  backend/src/Api/Background/TriageWorker.cs \
  backend/src/Api/Controllers/TriageController.cs
git commit -m "feat(s3): expose the triage and agent orchestration endpoints"
git push -u origin feature/s3-agent-orchestration
```

**Gate:** Every endpoint returns 2xx in Swagger

## Phase 8 — Backend tests

- `backend/tests/UnitTests/CaseSlaProcessorTests.cs`
- `backend/tests/UnitTests/ChatCompletionsLlmClientTests.cs`
- `backend/tests/UnitTests/NotificationServiceTests.cs`
- `backend/tests/UnitTests/OllamaClientTests.cs`
- `backend/tests/UnitTests/TriageOrchestratorEmergencyTests.cs`
- `backend/tests/UnitTests/TriageOrchestratorSchemaTests.cs`
- `backend/tests/UnitTests/TriageWorkerRecoveryTests.cs`

```bash
git add \
  backend/tests/UnitTests/CaseSlaProcessorTests.cs \
  backend/tests/UnitTests/ChatCompletionsLlmClientTests.cs \
  backend/tests/UnitTests/NotificationServiceTests.cs \
  backend/tests/UnitTests/OllamaClientTests.cs \
  backend/tests/UnitTests/TriageOrchestratorEmergencyTests.cs \
  backend/tests/UnitTests/TriageOrchestratorSchemaTests.cs \
  backend/tests/UnitTests/TriageWorkerRecoveryTests.cs
git commit -m "test(s3): cover the triage and agent orchestration service and policy layer"
git push -u origin feature/s3-agent-orchestration
```

**Gate:** `dotnet build` clean and `dotnet test` green — **backend gate**

## Phase 9 — React web application

- `web/src/components/layout/AmbientMesh.tsx`
- `web/src/components/layout/AppLayout.tsx`
- `web/src/components/shared/ListToolbar.tsx`
- `web/src/components/shared/Pagination.tsx`
- `web/src/components/shared/StatusBadge.tsx`
- `web/src/components/shared/ViewState.test.tsx`
- `web/src/components/shared/ViewState.tsx`
- `web/src/index.css`
- `web/src/pages/dashboard/DashboardPage.tsx`
- `web/src/styles/components.css`
- `web/src/styles/materials.css`
- `web/src/styles/tokens.css`

```bash
git add \
  web/src/components/layout/AmbientMesh.tsx \
  web/src/components/layout/AppLayout.tsx \
  web/src/components/shared/ListToolbar.tsx \
  web/src/components/shared/Pagination.tsx \
  web/src/components/shared/StatusBadge.tsx \
  web/src/components/shared/ViewState.test.tsx \
  web/src/components/shared/ViewState.tsx \
  web/src/index.css \
  web/src/pages/dashboard/DashboardPage.tsx \
  web/src/styles/components.css \
  web/src/styles/materials.css \
  web/src/styles/tokens.css
git commit -m "feat(s3): add the triage and agent orchestration web screens"
git push -u origin feature/s3-agent-orchestration
```

## Phase 10 — Flutter mobile application

- `mobile/lib/models/app_notification.dart`
- `mobile/lib/models/triage_case.dart`
- `mobile/lib/providers/cases_provider.dart`
- `mobile/lib/providers/notifications_provider.dart`
- `mobile/lib/providers/push_registration_provider.dart`
- `mobile/lib/screens/home/home_screen.dart`
- `mobile/lib/screens/notifications/notifications_screen.dart`
- `mobile/lib/screens/triage/case_status_screen.dart`
- `mobile/lib/screens/triage/cases_screen.dart`
- `mobile/lib/screens/triage/submit_complaint_screen.dart`
- `mobile/lib/services/api/patient_api.dart`
- `mobile/lib/theme/app_theme.dart`
- `mobile/lib/theme/glass.dart`
- `mobile/lib/widgets/shared/async_state_views.dart`
- `mobile/lib/widgets/shared/brand_mark.dart`
- `mobile/lib/widgets/shared/status_stepper.dart`
- `mobile/lib/widgets/shared/symptom_chip.dart`

```bash
git add \
  mobile/lib/models/app_notification.dart \
  mobile/lib/models/triage_case.dart \
  mobile/lib/providers/cases_provider.dart \
  mobile/lib/providers/notifications_provider.dart \
  mobile/lib/providers/push_registration_provider.dart \
  mobile/lib/screens/home/home_screen.dart \
  mobile/lib/screens/notifications/notifications_screen.dart \
  mobile/lib/screens/triage/case_status_screen.dart \
  mobile/lib/screens/triage/cases_screen.dart \
  mobile/lib/screens/triage/submit_complaint_screen.dart \
  mobile/lib/services/api/patient_api.dart \
  mobile/lib/theme/app_theme.dart \
  mobile/lib/theme/glass.dart \
  mobile/lib/widgets/shared/async_state_views.dart \
  mobile/lib/widgets/shared/brand_mark.dart \
  mobile/lib/widgets/shared/status_stepper.dart \
  mobile/lib/widgets/shared/symptom_chip.dart
git commit -m "feat(s3): add the triage and agent orchestration mobile screens"
git push -u origin feature/s3-agent-orchestration
```

## Phase 11 — Flutter tests

- `mobile/test/providers/patient_providers_test.dart`
- `mobile/test/screens/async_state_views_test.dart`
- `mobile/test/screens/case_status_screen_test.dart`
- `mobile/test/screens/home_screen_test.dart`

```bash
git add \
  mobile/test/providers/patient_providers_test.dart \
  mobile/test/screens/async_state_views_test.dart \
  mobile/test/screens/case_status_screen_test.dart \
  mobile/test/screens/home_screen_test.dart
git commit -m "test(s3): cover the triage and agent orchestration providers and screens"
git push -u origin feature/s3-agent-orchestration
```

**Gate:** `flutter analyze` clean and `flutter test` green

## ⚠ SHARED files you coordinate

You are the coordinator for these. Others add lines inside their own labelled blocks; you keep the file coherent and never reformat it.

- `backend/src/Application/Agents/AgentContracts.cs`
- `web/src/store/index.ts`

## Your agentic contribution (12 marks)

**Coordinator + Context + Analysis agents** — the orchestration that assembles a case file. The agent subsystem is only ever called by ASP.NET Core, never by a client (invariant 3).

## Phase 12 — documentation and evidence

| Stage | Commit |
|---|---|
| `docs/individual-reports/S3.md` §1–§4 | `docs(s3): complete report sections 1 to 4 with evidence` |
| `docs/individual-reports/S3.md` §5–§7 + `git log --author` output | `docs(s3): complete report sections 5 to 7 with evidence` |
| `docs/ai-disclosure/S3.md` | `docs(s3): finalise the AI use disclosure log` |
| `docs/individual-reports/S3.md` §8 — **written by you, never AI** | `docs(s3): add personal reflection` |

> §8 must be your own words. An AI-generated reflection receives no credit.

## Summary

| | |
|---|---|
| Files you own | 57 |
| ⚠ SHARED files you coordinate | 2 |
| Database tables | `episodes` `triage_cases` `agent_traces` `notification_subscriptions` |
| Endpoints | `/episodes/*` `/triage-cases/*` `/agent-traces/*` `/notifications/*` |
| Commits in this plan | 12 phases, ~16 commits minimum |

## Verify before you call it done

```bash
# your commits, and only yours
git log --author="Karunathilaka" --oneline | wc -l

# nothing of yours is untagged or unowned
grep -E '^S3\s' docs/OWNERSHIP.tsv | cut -f2 | \
  xargs -I{} git log -1 --format="%an  {}" -- {}

# full stack still builds
cd backend && dotnet build && dotnet test
cd ../web && npm ci && npm run build && npx vitest run
cd ../mobile && flutter pub get && flutter analyze && flutter test
```

